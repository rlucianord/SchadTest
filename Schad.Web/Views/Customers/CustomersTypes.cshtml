@model IEnumerable<Schad.Models.Data.CustomerType>

@{
    ViewBag.Title = "List";
}

<h2>List</h2>

<p>

</p>
<button type="button" value="Save" class="btn btn-default">

    @Html.ActionLink("Create New", "CreateCustomersTypes")
</button>
<button type="button" value="" class="btn btn-warning">

    @Html.ActionLink("Clients List", "Index")
</button>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Description)
        </th>
       
        @*<th></th>*@
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Description)
            </td>
            
            @*<td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>*@
        </tr>
    }

</table>

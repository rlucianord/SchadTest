@model IEnumerable<Schad.Models.Data.Customer>

@{
    ViewBag.Title = "List";
}

<h2>Index</h2>

<p>
   
</p>
<button type="button" value="Save" class="btn btn-default">

    @Html.ActionLink("Create New", "Create")
</button>

<button type="button" value="Save" class="btn btn-default">

    @Html.ActionLink("Create Type", "CreateCustomersTypes")
</button>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CustomerType.Description)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CustName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerType.Description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CustName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
